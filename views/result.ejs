<!DOCTYPE html>
<html lang="km">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Result</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kantumruy+Pro:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Kantumruy Pro', sans-serif; }
        .animate-pop { animation: pop 0.5s cubic-bezier(0.26, 0.53, 0.74, 1.48) forwards; }
        @keyframes pop { 0% { transform: scale(0.5); opacity: 0; } 100% { transform: scale(1); opacity: 1; } }
    </style>
</head>
<body class="bg-[#f1f5f9] min-h-screen flex items-center justify-center p-6">

    <div class="max-w-md w-full bg-white rounded-3xl shadow-[0_20px_50px_rgba(0,0,0,0.05)] overflow-hidden animate-pop">
        
        <div class="h-32 flex items-center justify-center 
            <%= color === 'blue' ? 'bg-blue-600' : '' %>
            <%= color === 'green' ? 'bg-emerald-500' : '' %>
            <%= color === 'red' ? 'bg-rose-500' : '' %>
            <%= color === 'orange' ? 'bg-amber-500' : '' %>">
            
            <% if (color === 'green' || color === 'blue') { %>
                <svg class="w-20 h-20 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            <% } else if (color === 'red') { %>
                <svg class="w-20 h-20 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            <% } else { %>
                <svg class="w-20 h-20 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                </svg>
            <% } %>
        </div>

        <div class="p-8 text-center">
            <h1 class="text-2xl font-bold mb-3 
                <%= color === 'blue' ? 'text-blue-600' : '' %>
                <%= color === 'green' ? 'text-emerald-600' : '' %>
                <%= color === 'red' ? 'text-rose-600' : '' %>
                <%= color === 'orange' ? 'text-amber-600' : '' %>">
                <%= title %>
            </h1>
            
            <p class="text-gray-600 leading-relaxed mb-8">
                <%= message %>
            </p>

            <a href="/register" class="inline-block w-full py-4 rounded-2xl font-semibold text-white transition-all duration-300 transform hover:-translate-y-1 shadow-lg
                <%= color === 'blue' ? 'bg-blue-600 shadow-blue-200 hover:bg-blue-700' : '' %>
                <%= color === 'green' ? 'bg-emerald-500 shadow-emerald-200 hover:bg-emerald-600' : '' %>
                <%= color === 'red' ? 'bg-rose-500 shadow-rose-200 hover:bg-rose-600' : '' %>
                <%= color === 'orange' ? 'bg-amber-500 shadow-amber-200 hover:bg-amber-600' : '' %>">
                យល់ព្រម / OK
            </a>
            
            <p class="mt-6 text-xs text-gray-400">Student Event Management System v2.0</p>
        </div>
    </div>

</body>
</html>