<!DOCTYPE html>
<html lang="km">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ប្រវត្តិរូប និងកាតវត្តមាន - Student Pass</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kantumruy+Pro:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        body { 
            font-family: 'Kantumruy Pro', sans-serif; 
            background: radial-gradient(circle at top right, #f1f5f9, #e2e8f0);
            min-height: 100vh;
        }
        .glass-container { 
            background: rgba(255, 255, 255, 0.9); 
            backdrop-filter: blur(20px); 
            border: 1px solid rgba(255, 255, 255, 0.4);
        }
        .qr-card {
            background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
            border: 2px dashed #e2e8f0;
        }
        .custom-input {
            transition: all 0.2s ease-in-out;
            border: 1.5px solid #e2e8f0;
        }
        .custom-input:focus {
            border-color: #6366f1;
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.1);
            background: white;
        }
        .btn-gradient {
            background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%);
        }
    </style>
</head>
<body class="flex items-center justify-center p-4 md:p-10">

    <div class="max-w-2xl w-full space-y-8">
        
        <div class="glass-container rounded-[2.5rem] p-8 md:p-10 shadow-2xl text-center relative overflow-hidden">
            <div class="absolute top-0 right-0 w-32 h-32 bg-indigo-500/5 rounded-full -mr-16 -mt-16"></div>
            
            <div class="relative z-10">
                <span class="inline-block px-4 py-1.5 rounded-full bg-indigo-50 text-indigo-600 text-[10px] font-black uppercase tracking-widest mb-6">Digital Attendance Pass</span>
                
                <h2 class="text-2xl font-black text-slate-800 mb-1"><%= student.name_kh %></h2>
                <p class="text-slate-400 text-xs font-bold uppercase tracking-tighter mb-8"><%= student.name_en %></p>

                <div class="qr-card inline-block p-6 rounded-[2rem] bg-white shadow-inner mb-6 relative group">
                    <img id="qrImage" src="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=<%= student._id %>" 
                         alt="Attendance QR" 
                         class="mx-auto w-[220px] h-[220px]">
                </div>

                <div class="flex flex-col items-center gap-4">
                    <button onclick="downloadQR()" class="flex items-center gap-2 px-6 py-3 bg-white border border-slate-200 text-slate-600 font-bold rounded-2xl shadow-sm hover:bg-slate-50 transition-all active:scale-95 text-xs">
                        <svg class="w-4 h-4 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                        ទាញយករូបភាព QR (DOWNLOAD)
                    </button>

                    <div class="flex items-center justify-center gap-2 text-emerald-500 bg-emerald-50 py-3 px-6 rounded-2xl w-fit mx-auto">
                        <svg class="w-5 h-5 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        <span class="text-xs font-black uppercase">បង្ហាញ QR នេះដើម្បីកត់វត្តមាន</span>
                    </div>
                </div>
            </div>
        </div>

        <div class="glass-container rounded-[2.5rem] shadow-xl overflow-hidden">
            <div class="p-10 pb-6">
                <div class="flex items-center gap-4 mb-2">
                    <div class="p-2 bg-slate-100 rounded-lg text-slate-500">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path></svg>
                    </div>
                    <h3 class="text-xl font-black text-slate-800 tracking-tight">កែប្រែព័ត៌មាន</h3>
                </div>
                <p class="text-slate-400 text-sm">ប្អូនអាចកែប្រែព័ត៌មានសិក្សា និងទំនាក់ទំនងបានទីនេះ</p>
            </div>

            <form id="updateForm" class="px-10 pb-10 space-y-6">
                <input type="hidden" name="id" value="<%= student._id %>">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="block text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2 px-1">ឈ្មោះខ្មែរ</label>
                        <input type="text" name="name_kh" value="<%= student.name_kh %>" required 
                            class="custom-input w-full px-5 py-3.5 rounded-2xl bg-slate-50/50 outline-none font-bold text-slate-700">
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2 px-1">English Name</label>
                        <input type="text" name="name_en" value="<%= student.name_en %>" required 
                            class="custom-input w-full px-5 py-3.5 rounded-2xl bg-slate-50/50 outline-none font-black text-slate-700 uppercase">
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2 px-1">លេខទូរស័ព្ទ</label>
                        <input type="tel" name="phone" value="<%= student.phone %>" required 
                            class="custom-input w-full px-5 py-3.5 rounded-2xl bg-slate-50/50 outline-none font-bold text-slate-700">
                    </div>
                    <div>
                        <label class="block text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2 px-1">ភេទ</label>
                        <select name="gender" class="custom-input w-full px-5 py-3.5 rounded-2xl bg-slate-50/50 outline-none font-bold text-slate-700 appearance-none">
                            <option value="ប្រុស" <%= student.gender === 'ប្រុស' ? 'selected' : '' %>>ប្រុស / Male</option>
                            <option value="ស្រី" <%= student.gender === 'ស្រី' ? 'selected' : '' %>>ស្រី / Female</option>
                        </select>
                    </div>
                    <div class="md:col-span-2">
                        <label class="block text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2 px-1">មកពីវិទ្យាល័យ</label>
                        <input type="text" name="high_school" value="<%= student.high_school %>" 
                            class="custom-input w-full px-5 py-3.5 rounded-2xl bg-slate-50/50 outline-none font-bold text-slate-700">
                    </div>
                </div>

                <div class="pt-4 space-y-4">
                    <button type="submit" class="btn-gradient w-full py-4 text-white font-black rounded-2xl shadow-xl shadow-indigo-100 hover:shadow-indigo-200 transition-all active:scale-[0.98] flex items-center justify-center gap-3">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path></svg>
                        រក្សាទុកការផ្លាស់ប្តូរ
                    </button>
                    <a href="/" class="flex items-center justify-center gap-2 text-sm font-bold text-slate-400 hover:text-indigo-600 transition-all group">
                        <svg class="w-4 h-4 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
                        ចាកចេញពីទំព័រនេះ
                    </a>
                </div>
            </form>
        </div>
    </div>

    <script>
        // 📥 Logic ទាញយករូបភាព QR ឱ្យ "ហាប់ណែន" ១០០%
        async function downloadQR() {
    try {
        Swal.fire({ title: 'កំពុងបង្កើតកាតសិស្ស...', allowOutsideClick: false, didOpen: () => { Swal.showLoading(); } });

        const canvas = document.createElement('canvas');
        const ctx = canvas.getContext('2d');
        
        // ១. កំណត់ទំហំកាត (ឧទាហរណ៍៖ 600x800 pixel ឱ្យវាច្បាស់)
        canvas.width = 600;
        canvas.height = 850;

        // ២. គូរ Background ពណ៌ស និងមានរង្វង់មូលជុំវិញ (Rounded)
        ctx.fillStyle = "#ffffff";
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        
        // ថែមពណ៌ Indigo បន្តិចនៅខាងលើឱ្យស្អាត
        ctx.fillStyle = "#4f46e5";
        ctx.fillRect(0, 0, canvas.width, 150);

        // ៣. សរសេរអត្ថបទ (Text Details)
        ctx.textAlign = "center";
        
        // ចំណងជើង
        ctx.fillStyle = "#ffffff";
        ctx.font = "bold 30px 'Kantumruy Pro'";
        ctx.fillText("STUDENT ATTENDANCE PASS", canvas.width / 2, 85);

        // ឈ្មោះខ្មែរ
        ctx.fillStyle = "#1e293b";
        ctx.font = "bold 45px 'Kantumruy Pro'";
        ctx.fillText("<%= student.name_kh %>", canvas.width / 2, 250);

        // ឈ្មោះអង់គ្លេស
        ctx.fillStyle = "#64748b";
        ctx.font = "bold 25px sans-serif";
        ctx.fillText("<%= student.name_en %>".toUpperCase(), canvas.width / 2, 300);

        // វិទ្យាល័យ
        ctx.fillStyle = "#94a3b8";
        ctx.font = "300 22px 'Kantumruy Pro'";
        ctx.fillText("វិទ្យាល័យ៖ <%= student.high_school %>", canvas.width / 2, 350);

        // ៤. ទាញយក QR Code មកគូរលើ Canvas
        const qrUrl = $('#qrImage').attr('src');
        const qrImage = new Image();
        qrImage.crossOrigin = "anonymous"; // ការពារកុំឱ្យគាំងរឿង Security
        qrImage.src = qrUrl;

        qrImage.onload = function() {
            // គូរ QR នៅចំកណ្ដាល
            ctx.drawImage(qrImage, 125, 400, 350, 350);

            // ថែម Footer បន្តិច
            ctx.fillStyle = "#cbd5e1";
            ctx.font = "16px sans-serif";
            ctx.fillText("Generated by Student Event System v2.0", canvas.width / 2, 800);

            // ៥. បម្លែងទៅជារូបភាព រួចទាញយក
            const finalImage = canvas.toDataURL("image/png");
            const a = document.createElement('a');
            a.href = finalImage;
            a.download = `Pass-<%= student.name_en %>.png`;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            
            Swal.close();
        };

    } catch (err) {
        console.error(err);
        Swal.fire({ title: 'បរាជ័យ!', text: 'មានបញ្ហាបច្ចេកទេស!', icon: 'error' });
    }
}
    </script>
</body>
</html>