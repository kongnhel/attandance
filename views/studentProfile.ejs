<!DOCTYPE html>
<html lang="km">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ប្រវត្តិរូបសិស្ស - Ultimate UX</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kantumruy+Pro:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        body { 
            font-family: 'Kantumruy Pro', sans-serif; 
            background: radial-gradient(circle at top right, #eef2ff, #f8fafc);
        }
        .glass-container { 
            background: rgba(255, 255, 255, 0.8); 
            backdrop-filter: blur(20px); 
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        .input-group:focus-within label { color: #4f46e5; }
        .custom-input {
            transition: all 0.2s ease-in-out;
            border: 1.5px solid #e2e8f0;
        }
        .custom-input:focus {
            border-color: #6366f1;
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.1);
            background: white;
        }
        .btn-gradient {
            background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-6">

    <div class="fixed top-[-10%] left-[-10%] w-[40%] h-[40%] bg-indigo-100/50 rounded-full blur-[120px] -z-10"></div>
    <div class="fixed bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-blue-100/50 rounded-full blur-[120px] -z-10"></div>

    <div class="max-w-2xl w-full glass-container rounded-[2.5rem] shadow-[0_32px_64px_-16px_rgba(0,0,0,0.08)] overflow-hidden">
        
        <div class="relative p-10 pb-6 text-center">
            <div class="inline-flex p-4 rounded-3xl bg-indigo-50 text-indigo-600 mb-6 shadow-sm border border-indigo-100/50">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
            </div>
            <h2 class="text-3xl font-extrabold text-gray-900 tracking-tight">ព័ត៌មានផ្ទាល់ខ្លួន</h2>
            <p class="text-gray-500 mt-2 text-sm">គ្រប់គ្រង និងធ្វើបច្ចុប្បន្នភាពគណនីរបស់ប្អូន</p>
        </div>

        <form id="updateForm" class="px-10 pb-10 space-y-8">
            <input type="hidden" name="id" value="<%= student._id %>">
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-6">
                
                <div class="input-group">
                    <label class="block text-[11px] font-black text-gray-400 uppercase tracking-widest mb-2 px-1">ឈ្មោះខ្មែរ</label>
                    <input type="text" name="name_kh" value="<%= student.name_kh %>" required 
                        class="custom-input w-full px-5 py-3.5 rounded-2xl bg-gray-50/50 outline-none font-semibold text-gray-700">
                </div>

                <div class="input-group">
                    <label class="block text-[11px] font-black text-gray-400 uppercase tracking-widest mb-2 px-1">English Name</label>
                    <input type="text" name="name_en" value="<%= student.name_en %>" required 
                        class="custom-input w-full px-5 py-3.5 rounded-2xl bg-gray-50/50 outline-none font-bold text-gray-700 uppercase">
                </div>

                <div class="input-group">
                    <label class="block text-[11px] font-black text-gray-400 uppercase tracking-widest mb-2 px-1">លេខទូរស័ព្ទ</label>
                    <input type="tel" name="phone" value="<%= student.phone %>" required 
                        class="custom-input w-full px-5 py-3.5 rounded-2xl bg-gray-50/50 outline-none font-semibold text-gray-700">
                </div>

                <div class="input-group">
                    <label class="block text-[11px] font-black text-gray-400 uppercase tracking-widest mb-2 px-1">ភេទ</label>
                    <select name="gender" class="custom-input w-full px-5 py-3.5 rounded-2xl bg-gray-50/50 outline-none font-semibold text-gray-700 appearance-none">
                        <option value="ប្រុស" <%= student.gender === 'ប្រុស' ? 'selected' : '' %>>ប្រុស / Male</option>
                        <option value="ស្រី" <%= student.gender === 'ស្រី' ? 'selected' : '' %>>ស្រី / Female</option>
                    </select>
                </div>

                <div class="md:col-span-2 input-group">
                    <label class="block text-[11px] font-black text-gray-400 uppercase tracking-widest mb-2 px-1">មកពីវិទ្យាល័យ</label>
                    <div class="relative">
                        <input type="text" name="high_school" value="<%= student.high_school %>" 
                            class="custom-input w-full px-5 py-3.5 rounded-2xl bg-gray-50/50 outline-none font-semibold text-gray-700">
                        <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-300">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
                        </span>
                    </div>
                </div>
            </div>

            <div class="pt-4 space-y-4">
                <button type="submit" class="btn-gradient w-full py-4 text-white font-bold rounded-2xl shadow-xl shadow-indigo-100 hover:shadow-indigo-200 transition-all active:scale-[0.98] flex items-center justify-center gap-3">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                    រក្សាទុកការផ្លាស់ប្តូរ (SAVE CHANGES)
                </button>
                
                <a href="/" class="flex items-center justify-center gap-2 text-sm font-bold text-gray-400 hover:text-indigo-600 transition-all group">
                    <svg class="w-4 h-4 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
                    ត្រឡប់ទៅកាន់ទំព័រដើម
                </a>
            </div>
        </form>
    </div>

    <script>
        $('#updateForm').on('submit', function(e) {
            e.preventDefault();
            const id = $('input[name="id"]').val();
            
            // បង្ហាញ Loading បន្តិចឱ្យសមជា App ថ្លៃ
            Swal.fire({
                title: 'កំពុងរក្សាទុក...',
                allowOutsideClick: false,
                didOpen: () => { Swal.showLoading(); }
            });

            $.ajax({
                type: 'PUT',
                url: '/student/profile/update/' + id,
                data: $(this).serialize(),
                success: function(res) {
                    Swal.fire({ 
                        title: 'ជោគជ័យ!', 
                        text: res.message, 
                        icon: 'success', 
                        confirmButtonColor: '#4f46e5',
                        customClass: { popup: 'rounded-3xl' }
                    });
                },
                error: function() {
                    Swal.fire({ 
                        title: 'បរាជ័យ!', 
                        text: 'មិនអាចកែប្រែបានទេ! សូមឆែកមើលអ៊ីនធឺណិតបង! 😂', 
                        icon: 'error',
                        confirmButtonColor: '#ef4444'
                    });
                }
            });
        });
    </script>
</body>
</html>